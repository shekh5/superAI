services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY}:latest
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - REDIS_URL=redis://redis:6379/0
      - GOOGLE_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL
      - WEB_SEARCH_MODEL
      - REACT_TEMPERATURE
      - SUMMARY_TEMPERATURE
      - WEATHER_API_KEY
      - WEATHER_FAILURE_RATE
      - CALCULATOR_BAD_INPUT_RATE
      - CONTEXT_INPUT_TOKEN_BUDGET
      - CONTEXT_OUTPUT_TOKEN_RESERVE
      - CONTEXT_TOKEN_SAFETY_MARGIN
      - CONTEXT_RECENT_MESSAGES
      - CONTEXT_RECENT_HIGH_WATERMARK
      - CONTEXT_HIGH_PRIORITY_MESSAGES
      - CONTEXT_LIGHT_COMPRESSION_RATIO
      - CONTEXT_STRONG_COMPRESSION_RATIO
      - CONTEXT_CRITICAL_RATIO
      - CONTEXT_TOOL_OUTPUT_MAX_TOKENS
      - SESSION_MAX_MESSAGES
      - SESSION_TTL_SECONDS
      - DOCUMENT_MAX_FILE_BYTES
      - DOCUMENT_MAX_UNITS
      - DOCUMENT_MAX_EXTRACTED_CHARS
      - DOCUMENT_CHUNK_WORDS
      - DOCUMENT_CHUNK_OVERLAP_WORDS
      - DOCUMENT_RETRIEVAL_CHUNKS
      - DOCUMENT_RETRIEVAL_MAX_CHARS
      - DOCUMENT_MAX_PER_SESSION
      - DOCUMENT_TTL_SECONDS
      - EVIDENCE_WORKSPACE_ENABLED
      - AUTH_REQUIRED
      - DATABASE_URL
      - S3_BUCKET
      - AWS_REGION
      - COOKIE_SECURE
      - BOOTSTRAP_ADMIN_EMAIL
      - BOOTSTRAP_ADMIN_PASSWORD
      - EMBEDDING_MODEL
      - EVIDENCE_SUPPORT_THRESHOLD
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  worker:
    image: ghcr.io/${GITHUB_REPOSITORY}:latest
    command: python -m reasoning_chain.worker
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL
      - S3_BUCKET
      - AWS_REGION
      - EMBEDDING_MODEL
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  redis_data:

# Used for DEPLOYMENT (EC2/server), not local dev.
# Difference from docker-compose.yml:
#   - "image:" pulls a pre-built image from GHCR instead of "build: ."
#   - No public port for redis — only the app container talks to it
#     internally over the Compose network, so it's not exposed externally.
#
# GITHUB_REPOSITORY should be set as an env var before running, e.g.:
#   export GITHUB_REPOSITORY=your-username/your-repo
#   docker compose -f docker-compose.prod.yml up -d
