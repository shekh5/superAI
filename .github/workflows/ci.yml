name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"  # caches pip downloads between runs, speeds up CI

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Lint with ruff
        run: ruff check app reasoning_chain tests

      - name: Run tests
        run: pytest -v --tb=short

  docker-build-check:
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image (no push, just verify it builds)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: agentic-capstone:ci-check
          build-args: |
            BAKE_EMBEDDING_MODEL=false
